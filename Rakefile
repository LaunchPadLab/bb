# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project/template/ios'
require 'bubble-wrap'
require 'bubble-wrap/location'

Motion::Project::App.setup do |app|
  # Use `rake config' to see complete project settings.
  app.name = "Brewers Barrel"
  app.fonts = ["Inconsolata.ttf"]
  app.icons = ["icon.png"]
  app.deployment_target = '6.0'
  app.version = "0.1"
  app.identifier = "com.launchpadlab.brewersbarrel"
  app.device_family = :iphone  
  app.codesign_certificate = 'iPhone Distribution: Thomas Cullen (L69M2TAK6D)' 
  app.provisioning_profile = '/Users/bhennes2/iPhone_BB.mobileprovision'
end